# FREE VERSION - No Paid APIs Required!

# Application Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=True
CHROME_BIN=/usr/bin/chromium

# Optional: OpenAI API (Only if you want GPT summarization)
# OPENAI_API_KEY=your_openai_api_key_here

# Optional: PimEyes Credentials (Only if you have a paid account)
# PIMEYES_USERNAME=your_pimeyes_username
# PIMEYES_PASSWORD=your_pimeyes_password

# Optional: Proxy Configuration (For enhanced web scraping)
USE_PROXIES=False
PROXY_LIST_URL=https://api.proxyscrape.com/v2/?request=get&protocol=http&timeout=10000&country=all&ssl=all&anonymity=all

# Note: This system now uses FREE alternatives:
# - Web scraping instead of paid APIs
# - Manual search links for verification
# - Local summarization instead of GPT (optional)
# - Enhanced error handling and fallbacks 